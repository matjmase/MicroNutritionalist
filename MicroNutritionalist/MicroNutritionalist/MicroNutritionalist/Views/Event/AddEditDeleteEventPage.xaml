<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MicroNutritionalist.Views.Event.AddEditDeleteEventPage">
    <ContentPage.Content>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Button Grid.Row="1" Text="Add Event" Command="{Binding AddEventClicked}"/>
            <Button Grid.Row="3" Text="Edit Event" Command="{Binding EditEventClicked}"/>
            <Button Grid.Row="5" Text="Delete Event" Command="{Binding DeleteEventClicked}"/>
        </Grid>
    </ContentPage.Content>
</ContentPage>
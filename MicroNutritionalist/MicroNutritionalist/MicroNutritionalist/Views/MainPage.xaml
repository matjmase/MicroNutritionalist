<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MicroNutritionalist.Views.MainPage"
             Title="{Binding Title}">

    <ContentPage.Content>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Button Grid.Row="1" Text="Product" Command="{Binding ProductClicked}"/>
            <Button Grid.Row="3" Text="Event" Command="{Binding EventClicked}"/>
            <Button Grid.Row="5" Text="Analytics" Command="{Binding AnalyticsClicked}"/>
        </Grid>
    </ContentPage.Content>

</ContentPage>